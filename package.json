{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "run dev",
    "build": "run build",
    "start": "run start",
    "lint": "run lint"
  },
  "dependencies": {
    "express": "latest",
    "mongoose": "latest",
    "dotenv": "latest"
  }
}